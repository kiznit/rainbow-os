# Bochs configuration file

# Hardware
ata0-master: type=disk, path="./build/$ARCH/rainbow.img", mode=flat
boot: disk
mouse: enabled=1
megs: 2048

# Output log to a file
log: bochsout.txt

# Break on xchg bx, bx
magic_break: enabled=1

# Display
display_library: sdl, options="gui_debug"
