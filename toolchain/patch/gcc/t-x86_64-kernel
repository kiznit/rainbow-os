# We need:
#   EFI bootloader: fpic mno-red-zone
#   Kernel        : mno-red-zone mcmodel=kernel
#   User space    : fpic and <nothing>

MULTILIB_OPTIONS    = fpic mcmodel=kernel mno-red-zone
MULTILIB_DIRNAMES   = pic kernel no-red-zone

# Exclude what we don't need
MULTILIB_EXCEPTIONS += mcmodel=kernel
MULTILIB_EXCEPTIONS += mno-red-zone
MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel
MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel/mno-red-zone


# We need:
#   EFI bootloader: fpic mno-red-zone
#   Kernel        : mcmodel=kernel mgeneral-regs-only mno-red-zone
#   User space    : fpic and <nothing>

#MULTILIB_OPTIONS    = fpic mcmodel=kernel mno-red-zone mgeneral-regs-only
#MULTILIB_DIRNAMES   = pic kernel no-red-zone no-float

# Exclude what we don't need
#MULTILIB_EXCEPTIONS += mcmodel=kernel
#MULTILIB_EXCEPTIONS += mcmodel=kernel/mgeneral-regs-only
#MULTILIB_EXCEPTIONS += mcmodel=kernel/mno-red-zone
#MULTILIB_EXCEPTIONS += mgeneral-regs-only
#MULTILIB_EXCEPTIONS += mno-red-zone
#MULTILIB_EXCEPTIONS += mno-red-zone/mgeneral-regs-only
#MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel
#MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel/mgeneral-regs-only
#MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel/mno-red-zone
#MULTILIB_EXCEPTIONS += fpic/mcmodel=kernel/mno-red-zone/mgeneral-regs-only
#MULTILIB_EXCEPTIONS += fpic/mgeneral-regs-only
#MULTILIB_EXCEPTIONS += fpic/mno-red-zone/mgeneral-regs-only
